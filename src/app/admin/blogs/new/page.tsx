import { BlogEditor } from "@/components/admin/BlogEditor";

export const metadata = {
    title: "New Post | MiniSpark Admin",
};

export default function NewAdminBlogPage() {
    return <BlogEditor />;
}
